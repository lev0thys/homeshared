{
  "name": "@homeshared/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schemas": "./src/schemas/index.ts",
    "./types": "./src/types/index.ts"
  },
  "scripts": {
    "typecheck": "node ../../node_modules/typescript/bin/tsc --noEmit",
    "lint": "echo \"(lint configuré au niveau racine)\" && exit 0",
    "test": "echo \"tests in apps/api\" && exit 0",
    "build": "echo \"shared sert en source, pas de build\" && exit 0"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vitest": "^2.1.3"
  }
}
